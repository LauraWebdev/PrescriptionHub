# Add project specific R8 rules here.
# AGP will combine all keep rule files in src/main/keepRules to pass to R8
#
# For more details, see
#   https://d.android.com/r/tools/r8/keep-rules

# If your project uses WebView with JS, uncomment the following
# and specify the fully qualified class name to the JavaScript interface
# class:
#-keepclassmembers class fqcn.of.javascript.interface.for.webview {
#   public *;
#}

# ScheduleType is reconstructed from a database string via valueOf(), so R8
# cannot see which constants are reachable. Keep their names.
-keepclassmembers enum media.laura.prescriptionhub.** {
    <fields>;
    public static **[] values();
    public static ** valueOf(java.lang.String);
}

# Keep crash reports mappable back to source via mapping.txt.
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile
